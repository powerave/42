<svg width="900" height="220" viewBox="0 0 900 220" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <filter id="neon-glow" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur stdDeviation="4" result="blur1"/>
      <feMerge>
        <feMergeNode in="blur1"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>
  <rect width="900" height="220" fill="#0d1117"/>
  <g filter="url(#neon-glow)" transform="translate(36, 30)">
    <!-- (colle ici tous les rects générés — je te donne le fichier complet ci-dessous si tu préfères) -->
  </g>
  <text x="50%" y="88%" dominant-baseline="middle" text-anchor="middle"
        fill="#00FF66" opacity="0.6" font-family="'Courier New', monospace" font-size="14" letter-spacing="4">
    // ACCESS_GRANTED :: WELCOME_TO_THE_MATRIX
  </text>
</svg>
